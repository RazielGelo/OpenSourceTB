doctype html
html(lang="en")
	head
		meta(charset="UTF-8")
		meta(http-equiv="X-UA-Compatible", content="IE=edge")
		meta(name="viewport", content="width=device-width, initial-scale=1.0")
		link(rel="stylesheet", href="/stylesheets/style.css")
		link(rel="stylesheet", href="/stylesheets/search.css")
		block script
		block title
			title OpenSourceTextbook
	header
		div
		if user
			p Welcome #{user.firstName}!
		div
			img(src="https://i.ibb.co/wBK49jh/Logo.png" alt="OpensourceTB" class="logo" style={height: "100px"})
			a(href='/') Home
				| 
				if user
					a(href='/users/profile') Profile
					| 
					a(href='/users/logout') Logout
				else 
					a(href='/login') Login
					| 
					a(href='/register') Register
	body
		!= messages('message', locals)
		if errors
			each error, i in errors
				div(class="alert alert-danger") #{error.msg}
		block content		
	footer
		p OpenSource Textbook empowers and connects authors and readers through open accessto a global reading and publishing platform.
		p Vancouver Community College, Vancouver, B.C., Canada
		p Copyright &copy; 2021
extends layout 

block content
	h1 Request Access
	p This is where you request access to the author.
	hr
	form(method='POST', action=`/books/request/${book.id}`)
		div
			label Message to Author: 
			textarea(name='request')
		div
			button(type="submit", class="buttonsmall") Send Request
			| 
			a(href=`/books/${book.id}`, class="buttonsmall") Cancel